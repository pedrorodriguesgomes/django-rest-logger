============
Installation
============

At the command line::

    $ easy_install django-rest-logger

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-rest-logger
    $ pip install django-rest-logger
